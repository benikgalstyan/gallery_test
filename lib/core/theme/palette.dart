import 'package:flutter/material.dart';

abstract class Palette {
  static const blueButtonColor = Color(0xFF00BFFF);
  static const greyButtonColor = Color(0xFF778899);
  static const greyTextColor = Colors.grey;
  static const pageColor = Colors.white;
}
